<!DOCTYPE html>
<html>
<head>
    <title>VR Knowledgebase</title>
    <!--<%= stylesheet_link_tag "/jqtouch/jqtouch.min.css", "/jqtouch/themes/jqt/theme.min.css" %>
    <%= javascript_include_tag "/jqtouch/jquery.1.3.2.min.js", "/jqtouch/jqtouch.min.js", "mobile" %> -->
    <%= stylesheet_link_tag "/iui/iui.css" %>
    <%= javascript_include_tag "/iui/iui.js" %>
    <meta name="viewport" content="width=device-width; initial-scale=1.0; maximum-scale=1.0; user-scalable=0;"/>
    <%= yield(:head) %>
</head>
    
<body>
		<!-- Top Toolbar -->
		<div class="toolbar">
			<a id="backButton" class="button" href="#"></a>
			<h1 id="pageTitle">VR Knowledgebase</h1>
			<% if user_signed_in? %>
				<%= link_to "Log out", destroy_user_session_path, :class => "button", :target => '_self' %>
			<% elsif admin_signed_in? %>
				<%= link_to "Log out", destroy_admin_session_path, :class => "button", :target => '_self' %>
			<% else %>
				<%= link_to "Log in", new_user_session_path, :class => "button", :target => '_self' %> 
			<% end %>
		</div>
		
		<!-- Home Menu -->
		<ul title="Home" selected="true">
			<% if user_signed_in? %><div><h3>Welcome, <%= current_user.email %></h3></div>
			<% else %>
				<div><h2>Welcome to the VR Knowledgebase</h2>
					<p>Use the links below to peruse the repository of immersion research, or sign in for more customization options. </p>
				</div>
			<% end %>
			<li class="group">Navigate the Knowledgebase</li>
			<li><a href="#About">About</a></li>
			<li><a href="#Search">Search</a></li>
			<li><a href="#Browse">Browse Categories</a></li>
			<% if user_signed_in? %>
				<li class="group">My Collections</li>
				<li><a href="#cont">Contributions</a></li>
				<li><a href="#comm">Comments</a></li>
				<li><a href="#favs">Favorites</a></li>
			<% end %>
		</ul>
				
		
		<!-- Search Sub-Menu -->
		<ul id="Search" title="Search" class="panel">
		</ul> 
		
		<!-- Browse Sub-Menu -->
		<ul id="Browse" title="Browse">		
			<li><a href="#comp">Components of Immersion</a></li>
			<li><a href="#task">Tasks</a></li>
			<li><a href="#sys">Systems</a></li>
					
		</ul>
		
		<!-- Browse by Components of Immersion -->
		<ul id="comp" title="Components of Immersion">
			<% @master_components.each do |c| %>
				<li><%= link_to c.tag, search_tag_path(:tag => c.tag) %> (<%= Entry.tagged_with(c.tag).size %>)</li>
			<% end %>
		</ul>
		
		<!-- Browse by Task -->	
		<ul id="task" title="Tasks">
			<% @master_tasks.each do |t| %>
				<li><%= link_to t.tag, search_tag_path(:tag => t.tag) %> (<%= Entry.tagged_with(t.tag).size %>)</li>
			<% end %>
		</ul>
		
		<!-- Browse by System-->	
		<ul id="sys" title="Systems">
			<% @master_systems.each do |s| %>
				<li><%= link_to s.tag, search_tag_path(:tag => s.tag) %> (<%= Entry.tagged_with(s.tag).size %>)</li>
			<% end %>
		</ul>

		<!-- Do stuff depending on who's logged in -->	
		<%= yield %>
</body>
</html>
